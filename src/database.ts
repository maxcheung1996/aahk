import React from 'react';
import { deleteDatabase } from 'workbox-core/_private';
import { sqlite, existingConn } from '../src/App';
import { createTablesNoEncryption, importTwoUsers } from '../src/Utils/noEncryptionUtils';

export const LocalDBProvider: React.FC = () => {


    return null;
}